# Purchase-intent-Prediction
Research Incubation project comparing a baseline SVM against a hybrid ANN‑SVM classifier (ANN as feature extractor + SVM classifier) for predicting customer response to a marketing campaign — 22 architectures benchmarked, best config validated statistically.
